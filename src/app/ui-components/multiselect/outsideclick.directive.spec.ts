import { JarvisOutsideclickDirective } from './jarvis-outsideclick.directive';

describe('JarvisOutsideclickDirective', () => {
  it('should create an instance', () => {
    const directive = new JarvisOutsideclickDirective();
    expect(directive).toBeTruthy();
  });
});
